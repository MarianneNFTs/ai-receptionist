export const metadata = {
    title: 'AI Receptionist Platform',
    description: 'Multi-tenant AI receptionist system with Lebanese Arabic support',
}

export default function RootLayout({ children }) {
    return (
          <html lang="en">
                <body>
                  {children}
                </body>body>
          </html>html>
        )
}</html>
